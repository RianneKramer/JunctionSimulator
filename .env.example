# Simulator Configuration

# Operating mode: standalone or connected
MODE=connected

# External Controller Configuration (only used in connected mode)
CONTROLLER_URL=http://localhost:5001
CONTROLLER_TRIGGER_ENDPOINT=/api/data
CONTROLLER_STATE_ENDPOINT=/api/data/state
CONTROLLER_TIMEOUT=5000

# Simulator Settings
CAR_SPAWN_INTERVAL=20000
LIGHT_CHECK_INTERVAL=1000
TRAFFIC_LIGHT_ID=1.1
